-- 국가 테이블 생성
create table semi_nation(
    NATIONID varchar(2) primary key,
    NATIONNAME varchar(50)
);
select *
		from semi_nation;
-- 국가 테이블 데이터 삽입
insert into semi_nation values ('01', '국내');
insert into semi_nation values ('02', '국외');
 
-- 작가 테이블 생성
create table semi_author(
    AUTHORID int auto_increment primary key,
    AUTHORNAME varchar(50),
    NATIONID varchar(2),
    AUTHORINTRO text,
    foreign key (NATIONID) references semi_nation(NATIONID)
);
alter table semi_author add regDate timestamp default now();
alter table semi_author add updateDate timestamp default now();
alter table semi_author auto_increment = 1;		
insert into semi_author(authorName, nationId, authorIntro) values('유홍준', '01', '작가 소개입니다' );
insert into semi_author(authorName, nationId, authorIntro) values('김난도', '01', '작가 소개입니다' );
insert into semi_author(authorName, nationId, authorIntro) values('폴크루그먼', '02', '작가 소개입니다' );    
select *
		from semi_author 
